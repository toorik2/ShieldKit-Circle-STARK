/**
 * Canonical version byte of the frozen sealed-creation-log relation descriptor.
 * V17 changes the proof and BCH product graph, not this inherited relation.
 */
export const LOCAL_WORD_RELATION_CONSTRUCTION_VERSION = 16 as const;
