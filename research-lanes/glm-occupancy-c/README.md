# Research lane: GLM occupancy C

**Work only here.** Constitution: [`RULES.md`](RULES.md). Named end: [`GOAL.md`](GOAL.md). Argument/vk: [`ARGUMENT.md`](ARGUMENT.md). Start: [`START.md`](START.md).

Copied from `research-lanes/sha-in-occupancy-c` at `e0b0de3` (fused N = leftover L0 C(z) + 144 B leftover-z `C_SHA`). That parent lock state is the **starting artifact**, not the product. vk changes if any RULES line changes. There is **no vk yet**.

## What this is

The GLM agent’s self-contained lab carrying the SHA-in-occupancy-C geometry forward: leaf↔nf↔amount lives in occupancy composition C at the same 36 queries, evaluated in the lock, inside one standard Electrum tx, full completeness, field+query+SZ+hash ≥ 100, silent unlocking. Sibling boxes (HASH_BIT 100 KB walks, 1 MB SHA-AIR fork) are evidence, not this product.

## Run

```bash
cd research-lanes/glm-occupancy-c
npm ci
npx tsc --noEmit
```

Chipnet only. Never mainnet.

## Outside this lane (do not edit for this track)

| Path | Use |
|---|---|
| `research-lanes/sha-in-occupancy-c` | direct parent (e0b0de3 fused-N lock); evidence |
| `research-lanes/ideal-bch-shielded-pool-stark` | HASH_BIT leftover walks; `5de68272…` evidence |
| `research-lanes/nonstandard-ideal-circle-stark` | 1 MB SHA AIR on own inputs |
| `research-lanes/envelope-b-standard` | 91 KB freeze evidence |
| `research-lanes/batch-exit-walkin` | do not edit for this track |
| `workspaces/any-amount` | frozen ABL snapshot |
| `research-lanes/bch-shielded-pool-design` | sealed Fv1 |
